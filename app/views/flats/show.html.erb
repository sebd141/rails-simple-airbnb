<div class="container">
  <h1><%= @flat.name %></h1>
  <%= @flat.description %>
  <%= @flat.name %>
  <%= @flat.address %>
  <%= @flat.price_per_night %>
  <%= @flat.number_of_guest %>
  <%= link_to "Edit '#{@flat.name}'", edit_flat_path(@flat) %>
  </div>

</div>
